<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.proj.buzinest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UserPostsBusiness"></activity>
        <activity android:name=".BusinessAccountSettings" />
        <activity android:name=".ProfileActivity" />
        <activity
            android:name=".AllUsers"
            android:parentActivityName=".ChatPage" />
        <activity android:name=".AccountSettings" />
        <activity android:name=".ChatPage" />
        <activity android:name=".BusinessProfile" />
        <activity android:name=".RegisterBusinessAddress" />
        <activity android:name=".RegisterBusiness" />
        <activity android:name=".ForgotPassword" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SigninActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StatusActivity"
            android:parentActivityName=".AccountSettings" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>